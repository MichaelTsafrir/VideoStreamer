export const webSocketPort = 3002;
export const webSocketURL = `ws://localhost:${webSocketPort}`;

export const serverPort = 3001;
export const serverAddress = `http://localhost:${serverPort}`;

export const appPort = 3000;
export const appAddress = `http://localhost:${appPort}`;